[package]
edition.workspace = true
name = "test_utils"
# Test helpers shared between packages; never published on its own.
publish = false
version = "0.1.0"

[dependencies]
assert_fs = "1"
assertables = "10"
log.workspace = true

# Lint configuration lives in the workspace root manifest.
[lints]
workspace = true
